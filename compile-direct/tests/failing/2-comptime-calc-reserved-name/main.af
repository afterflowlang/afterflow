calc: /std/math/calc

main: () {
    calc.new(
        "hypot(width, hypot)",
        calc.var("width", 3.0)
        calc.var("hypot", 4.0)
        calc.end,
        (@f64) {
            @exit(0)
        }
    )
}
