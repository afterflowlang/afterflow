calc: /std/math/calc
fmt: /std/fmt

calculated: (ok: (), value: @f64) {
    fmt.new(
        "%\n",
        fmt.f64(value) fmt.end,
        @write(ok: ok)
    )
}

main: () {
    calc.new(
        "width + height + width",
        calc.var("height", 4.0)
        calc.var("width", 3.0)
        calc.end,
        calculated(@exit(0))
    )
}
