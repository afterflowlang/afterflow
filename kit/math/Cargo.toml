[package]
name = "freestanding-math"
version = "0.1.0"
edition = "2021"
description = "Freestanding numerical routines for statically linked programs"
license = "Apache-2.0"

[lib]
crate-type = ["staticlib"]

[dependencies]
freestanding-runtime = { path = "../runtime" }
libm = "0.2.16"
