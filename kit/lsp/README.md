# language-server-protocol

`language-server-protocol` contains the generic JSON-RPC transport and protocol
types needed by a synchronous language server. It intentionally contains no
Afterflow or compiler behavior, so it can be released and reused independently.
