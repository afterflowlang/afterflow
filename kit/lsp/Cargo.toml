[package]
name = "language-server-protocol"
version = "0.1.0"
edition = "2021"
description = "Synchronous Language Server Protocol transport and interoperable protocol types"
license = "Apache-2.0"

[dependencies]
lsp-server = "0.7"
lsp-types = "0.95"
serde = "1"
serde_json = "1"
