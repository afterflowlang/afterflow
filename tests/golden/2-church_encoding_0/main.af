fmt: /std/fmt
str: @str
int: @int

zero: (f: (int, (int)), x: int, ok: (int)){
    ok(x)
}
main: () {
    zero((x: int, ok:(int)){
        @add(x, 10, ok)
    }, 0, (x: int){
        fmt.new("result: %", @exit(1), fmt.int(x) fmt.end, @write(ok: @exit(0)))
    })
}
