stdout: /stdout

str: @str
int: @int

// Builtin package members do not occupy the user namespace until imported.
write: (n: int, ok: ()){
    stdout.printf("Gotcha: %d", n, ok)
}
main: () {
    write(1, @exit(0))
}
