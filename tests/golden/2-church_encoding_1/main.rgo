stdout: /stdout

str: @str
int: @int

one: (f: (int, (int)), x: int, ok: (int)){
    f(x, ok)
}
main: () {
    one((x: int, ok:(int)){
        @add(x, 10, ok)
    }, 0, (x: int){
        stdout.printf("result: %d", x, @exit(0))
    })
}
