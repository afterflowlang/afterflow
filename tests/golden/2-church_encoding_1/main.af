fmt: /std/fmt
str: @str
int: @int

one: (f: (int, (int)), x: int, ok: (int)){
    f(x, ok)
}
main: () {
    one((x: int, ok:(int)){
        @add(x, 10, ok)
    }, 0, (x: int){
        fmt.new("result: %", @exit(1), fmt.int(x) fmt.end, @write(ok: @exit(0)))
    })
}
