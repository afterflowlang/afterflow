fmt: /std/fmt
str: @str
int: @int

greet: (name: str, count: int, ok: ()) {
    fmt.new("hello %, you have % messages\n",
        fmt.str(name)
        fmt.int(count)
        fmt.end, @write(ok: ok)
    )
}
main: () {
    greet("Alice", 3, @exit(0))
}
